---
title: 'REST API'
description: 'NovaCloud EIP REST API Reference'
---

## Overview

The NovaCloud EIP REST API provides programmatic access to enterprise data, AI processing, and system management capabilities.

### Base URL

```
https://your-domain.com/api
```

### Authentication

All API endpoints require Bearer token authentication:

```bash
Authorization: Bearer YOUR_API_TOKEN
```

Obtain your API token from the dashboard settings.

---

## Core API Sections

<CardGroup cols={2}>

<Card title="Data Endpoints" icon="database">
  Query and manipulate business data from PostgreSQL
</Card>

<Card title="AI & RAG Endpoints" icon="robot">
  Semantic search and AI processing via LangChain
</Card>

<Card title="Graph Endpoints" icon="share-nodes">
  Query relationships and network analysis via Neo4j
</Card>

<Card title="Monitoring Endpoints" icon="chart-line">
  Real-time server metrics and capacity tracking
</Card>

</CardGroup>

---

## Key Features

- **Real-time Data Access** - Live database queries via PostgREST
- **Semantic Search** - AI-powered search using LangChain + Qdrant
- **Graph Queries** - Complex relationship queries via Neo4j
- **File Operations** - Upload/download via Supabase Storage
- **Real-time WebSocket** - Live data synchronization
- **Edge Functions** - Custom serverless logic

---

## Rate Limiting

API requests are rate limited:

- **Standard tier** - 1,000 requests/hour
- **Premium tier** - 10,000 requests/hour
- **Enterprise** - Unlimited with SLA

Rate limit headers:
```
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 999
X-RateLimit-Reset: 1692864000
```

---

## Error Handling

All errors return standard HTTP status codes with JSON response:

```json
{
  "error": "Unauthorized",
  "code": "AUTH_001",
  "message": "Invalid or expired token",
  "timestamp": "2024-01-15T10:30:00Z"
}
```

### Common Status Codes

| Code | Meaning |
|------|---------|
| 200 | Success |
| 400 | Bad Request |
| 401 | Unauthorized |
| 403 | Forbidden |
| 404 | Not Found |
| 429 | Rate Limited |
| 500 | Server Error |

---

## Response Format

All responses follow a consistent format:

```json
{
  "success": true,
  "data": { },
  "error": null,
  "timestamp": "2024-01-15T10:30:00Z"
}
```

---

## Explore Endpoints

<Card
  title="Endpoint Examples"
  icon="code"
  href="/api-reference/endpoint/get"
>
  View complete endpoint documentation
</Card>
  }
]
```
